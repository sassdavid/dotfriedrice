This document will list the standalone scripts installed by DotFriedRice —
their purpose, source, and any relevant notes — once the list stabilizes.

For now, see `_install/default/scripts` for the authoritative source.
