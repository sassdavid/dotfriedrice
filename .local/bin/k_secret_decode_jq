#!/usr/bin/env bash

set -o errexit
set -o pipefail
set -o nounset

if (($# != 2)); then
  echo "Usage: $(basename "$0") <namespace> <secret-name>" >&2
  exit 1
fi

kubectl get secret -n "${1}" "${2}" -o json | jq -r '.data | map_values(@base64d)'
